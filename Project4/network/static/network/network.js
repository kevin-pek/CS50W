/*document.addEventListener("DOMContentLoaded", () => {
    document.querySelector(".edit").addEventListener("click", edit);
})

function follow() {
    // follow user whose profile is being viewed
    fetch('edit/' + this.dataset.id, {method: "GET"})
    //.then(location.reload())
} */
